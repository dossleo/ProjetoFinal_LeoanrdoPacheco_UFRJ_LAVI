dados tratados
    Ordem 1
        sensor 1
            rpm máquina | RMS | Max | ...| Sensor 1 | Defeito
        sensor2
        .
        .
        .
        concatenado.csv
        concatenado_normalizado.csv
    Ordem 2


def get_files(self, pasta:str) -> list
    files = os.listdir(pasta)
    return files

for pasta in pastas
    files = self.get_files(pasta)
    for file in files
        file = self.read_file()
        for sensor in sensores
            for ordem in oredens
                calcule alguma coisa
